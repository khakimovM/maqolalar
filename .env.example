# docker-compose (root) uchun PostgreSQL kreditallari.
# Serverda: `cp .env.example .env` va POSTGRES_PASSWORD ni kuchli qiymatga o'zgartiring.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_kuchli_parol
POSTGRES_DB=maqolalar
